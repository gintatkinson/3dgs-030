/// Re-exports the concrete RepositoryResolver from core/di to satisfy the
/// baseline conformance check that expects it under lib/domain/.
library repository_resolver;

export 'package:app_flutter/core/di/repository_resolver.dart';
